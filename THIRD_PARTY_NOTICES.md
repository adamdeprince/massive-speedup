# Third-Party Notices

`massive-speedup` is licensed under Apache-2.0.

The native extension can build against the following C++ libraries:

- `simdjson`, fetched from `https://github.com/simdjson/simdjson` at tag `v3.13.0`.
  The upstream project is available under Apache-2.0 or MIT; this project uses it
  under Apache-2.0.
- `rapidgzip`, fetched from `https://github.com/mxmlnkn/rapidgzip` at tag
  `rapidgzip-v0.16.0`. The upstream project includes Apache-2.0 and MIT license
  files; this project uses it under Apache-2.0.

Both dependencies are permissively licensed and compatible with this project's
Apache-2.0 license.
