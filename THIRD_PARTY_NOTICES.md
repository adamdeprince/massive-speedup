# Third-Party Notices

`massive-speedup` is licensed under Apache-2.0.

The native extension can build against the following C++ library:

- `rapidgzip`, fetched from `https://github.com/mxmlnkn/rapidgzip` at tag
  `rapidgzip-v0.16.0`. The upstream project includes Apache-2.0 and MIT license
  files; this project uses it under Apache-2.0.

This dependency is permissively licensed and compatible with this project's
Apache-2.0 license.
